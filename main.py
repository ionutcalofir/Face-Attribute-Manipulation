from starGAN import StarGAN
from dataset import Dataset
from utils import Utils

# model = StarGAN(batch_size=1)
# model.build()
# model.train()

# Dataset().build_dataset_celeba()
# ds = Dataset()
# ds.get_train_batch(16)
# ds.get_val_batch(16)
# ds.get_test_batch(16)

# ut = Utils()
# ut.next_batch_train(16)
# ut.next_batch_val(16)
# ut.next_batch_test(16)
# ut.y_target(7, 16)

# x, y, y_target = ut.next_batch_train(16)
# ut.show_img(ut.denormalize_img(x[0]))
